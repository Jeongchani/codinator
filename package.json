{
  "name": "codinator-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "latest",
    "prettier": "latest"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}