{
  "name": "codinator-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "latest",
    "prettier": "latest"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}